# Quote values that contain shell metacharacters (@ $ ( ) & ? =) so that
# start.sh can parse them safely. godotenv (config.go) also handles quotes.
DATABASE_URL="user:pass@tcp(colorado.iixcp.rumahweb.net:3306)/dbname?charset=utf8mb4&parseTime=True&loc=Local"
JWT_SECRET="b8c24e995c43903521d044509f6949416b97a5e58f2e80d0db2051bd284ee67a"
PORT=8080
SYNC_SERVER_URL="https://sosiomen.com"
# Owner credentials used by /backup/push & /backup/pull to log into the website API
SYNC_OWNER_EMAIL="owner@singgah.coffee"
SYNC_OWNER_PASSWORD="admin"
